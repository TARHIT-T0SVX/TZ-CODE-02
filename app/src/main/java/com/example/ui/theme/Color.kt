package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Minimalist Core Color Palette
val TZeronBgDark = Color(0xFF0D0D0F)           // Primary Background: #0D0D0F (Deep Dark Base)
val TZeronSurface = Color(0xFF1E1D22)          // Panel / Secondary Surface Background: #1E1D22
val TZeronSurfaceElevated = Color(0xFF26252B)  // Slightly elevated surface for active items
val TZeronSurfaceCard = Color(0xFF18171B)      // Recessed/Card surface
val TZeronBorder = Color(0xFF35343F)           // Minimalist subtle border
val TZeronBorderSubtle = Color(0xFF282730)     // Ultra-subtle divider

// Minimalist Monochromatic Tone Scale
val TZeronPrimary = Color(0xFFF3F4F6)          // Crisp clean white/silver
val TZeronPrimaryGlow = Color(0xFFFFFFFF)      // Pure white
val TZeronSecondary = Color(0xFFD1D5DB)        // Light neutral silver
val TZeronTertiary = Color(0xFF9CA3AF)         // Medium neutral slate

// Typography
val TZeronTextPrimary = Color(0xFFF9FAFB)      // High contrast text
val TZeronTextSecondary = Color(0xFF9CA3AF)    // Secondary muted label
val TZeronTextMuted = Color(0xFF6B7280)        // Dim placeholder text

// Functional Indicators
val TZeronSuccess = Color(0xFF10B981)          // Emerald green indicator
val TZeronWarning = Color(0xFFF59E0B)          // Amber indicator
val TZeronError = Color(0xFFEF4444)            // Muted red strictly for console error exceptions
val TZeronAccentBlue = Color(0xFF007ACC)       // VS Code Signature Blue

// Selection Highlighting (Blue Selection System)
val EditorSelectionBg = Color(0x4D007ACC)       // 30% alpha VS Code Selection Blue
val EditorHandleColor = Color(0xFF007ACC)

// VS Code Dark Theme Palette
val VSCodeTag = Color(0xFF569CD6)              // Blue
val VSCodeAttr = Color(0xFF9CDCFE)             // Light Blue
val VSCodeString = Color(0xFFCE9178)           // Orange-Brown
val VSCodeKeyword = Color(0xFFC586C0)          // Pink/Purple
val VSCodeComment = Color(0xFF6A9955)          // Green
val VSCodeFunction = Color(0xFFDCDCAA)         // Yellow
val VSCodeNumber = Color(0xFFB5CEA8)           // Light Green
val VSCodeClass = Color(0xFF4EC9B0)            // Teal

// One Dark Pro Palette
val OneDarkTag = Color(0xFFE06C75)             // Coral Red
val OneDarkAttr = Color(0xFFD19A66)            // Orange
val OneDarkString = Color(0xFF98C379)          // Green
val OneDarkKeyword = Color(0xFFC678DD)         // Purple
val OneDarkComment = Color(0xFF5C6370)         // Grey
val OneDarkFunction = Color(0xFF61AFEF)        // Cyan Blue
val OneDarkNumber = Color(0xFFE5C07B)          // Yellow

// Monokai Pro Palette
val MonokaiTag = Color(0xFFFF6188)             // Vibrant Red/Pink
val MonokaiAttr = Color(0xFF78DCE8)            // Cyan
val MonokaiString = Color(0xFFFFD866)          // Soft Yellow
val MonokaiKeyword = Color(0xFFFF6188)         // Pink
val MonokaiComment = Color(0xFF727072)         // Grey
val MonokaiFunction = Color(0xFFA9DC76)        // Lime Green
val MonokaiNumber = Color(0xFFAB9DF2)          // Lilac

// Dracula Palette
val DraculaTag = Color(0xFFFF79C6)             // Pink
val DraculaAttr = Color(0xFF50FA7B)            // Green
val DraculaString = Color(0xFFF1FA8C)          // Yellow
val DraculaKeyword = Color(0xFFFF79C6)         // Pink
val DraculaComment = Color(0xFF6272A4)         // Purple Grey
val DraculaFunction = Color(0xFF8BE9FD)        // Cyan
val DraculaNumber = Color(0xFFBD93F9)          // Purple

// Default Syntax Aliases (VS Code standard)
val SyntaxTag = VSCodeTag
val SyntaxAttr = VSCodeAttr
val SyntaxString = VSCodeString
val SyntaxKeyword = VSCodeKeyword
val SyntaxComment = VSCodeComment
val SyntaxFunction = VSCodeFunction
val SyntaxNumber = VSCodeNumber
val SyntaxPunctuation = Color(0xFF9CA3AF)

